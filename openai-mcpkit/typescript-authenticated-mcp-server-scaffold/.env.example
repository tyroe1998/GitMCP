# Copy to .env and fill in values before running the server

# OpenAI configuration
# Secret used for calls to the OpenAI API from MCP tools.
OPENAI_API_KEY=
# Identifier of the OpenAI Vector Store that backs the `search`/`fetch` tools.
VECTOR_STORE_ID=

# Auth0 / OAuth configuration
# Base URL of your Auth0 (or OIDC) tenant, e.g. https://tenant.us.auth0.com.
AUTH0_ISSUER=

# Server settings
# Local port the MCP server listens on during development.
PORT=8788
# Public URL clients use to reach the resource server; update when tunneling/deploying.
RESOURCE_SERVER_URL=http://localhost:8788
# Comma-separated list of OAuth audiences the server accepts on incoming tokens. This should match the audience you've set as default in Auth0.
JWT_AUDIENCES=
